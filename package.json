{
  "name": "dte-monitor",
  "version": "1.0.0",
  "description": "Monitor DTE Proveedor - Fenix Gold / Lince SA",
  "private": true,
  "scripts": {
    "build":        "mbt build",
    "deploy":       "cf deploy mta_archives/dte-monitor_1.0.0.mtar --delete-service-keys",
    "build-deploy": "npm run build && npm run deploy",
    "undeploy":     "cf undeploy dte-monitor --delete-services --delete-service-keys"
  },
  "devDependencies": {
    "mbt": "^1.2.0"
  }
}
